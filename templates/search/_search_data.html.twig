{{ form_start(form) }}
<div class="container">
  <div class="row">
    

      {{ form_start(form) }}
      {{ dump(form) }}
        {# <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search"> #}
        {# {{ form_label(form.location,  'Location',{'attr': {'class': 'form-labell'} } ) }} #}
        <div class="col-12 d-flex">
          {# {{ form_widget(form.location, {'attr': {'class': 'form-control'} } ) }} #}
          {# <button {{ form_widget(form.submit, {'attr': {'class': 'btn btn-outline-success btn-lg'}}) }}>Search</button> #}

          <button class="btn btn-primary" type="submit">Search</button>
        </div>
      {{ form_end(form) }}
    
  </div>
   
  </div>

{{ form_end(form) }}